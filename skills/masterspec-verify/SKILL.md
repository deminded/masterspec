---
name: masterspec-verify
description: >
  Вычитка артефактов фабрики по 5 осям. scope=req — слой требований (критерий spec_ready);
  scope=spec — слой спецификаций (+ O0 single-source, O6 контракт, O7 физмодель; критерий codegen_ready);
  scope=change — полнота каскада изменения. Оси — данные (../masterspec/references/patterns/verification-axes.md).
  Выдаёт дыры с severity + отчуждаемые метрики, флагует человеку. Поглощает прежний kernel-режим audit.
when_to_use: >
  проверить слой требований/спецификаций, аудит покрытия, verify scope=req|spec|change,
  найти дыры до кода, оценить codegen_ready / spec_ready
argument-hint: "scope=req|spec|change [preset=core|full]"
allowed-tools:
  - Read
  - Glob
  - Grep
  - Bash
  - Task
---

# masterspec-verify — вычитка по 5 осям

Read-only по отношению к артефактам: я нахожу дыры и считаю метрики, НЕ правлю (правки — `derive`/`evolve`). Оси и дешёвое ядро — `../masterspec/references/patterns/verification-axes.md`.

## Параметры
- `scope=req` — оси 1-5 над слоем требований. Критерий выхода: **spec_ready** (нет блокеров О1/О4b/О5 → требования годны как вход для спецификаций). Код-генерабельность тут НЕ проверяется — она на слое спеки.
- `scope=spec` — оси 1-5 + **O0 single-source (hard-fail при двух редакциях контракта)** + O6 контракт + O7 физмодель. Критерий: **codegen_ready**.
- `scope=change` — полнота каскада: каскад-вниз-к-верификации (правка/подъём → AC), обе стороны контракта, scope-fence (вердикт каждому соседу), правило немого вердикта и немого подъёма.

## Метод
1. preset=core (дефолт) — дешёвое ядро на каждом элементе: структполнота → глоссарий → матрица состояний → трасса событие→данные → оракул GWT. preset=full — все оси на слое + критичных элементах.
2. **Негатив-ось (5) — отдельным антагонистом** (`Task`-субагент с единственной задачей «найди что сломается / чего не хватает»): состязательность ловит false-positive, недоступный кооперативному ревью. Не самопроверка автора.
3. **Эталон, не «похоже на правду».** Ревьюер прогоняет спеку через конкретные тесты (GWT-оракул, матрица состояний, трасса), а не оценивает «выглядит ли полным». Против ложной зелёной галочки.
4. Для scope=change — независимость: считать каскад ЗАНОВО, не доверяя зоне генератора (слепой контроль ловит то, что самопроверка пропускает).

## Выход (R-METRICS, отчуждаемо от содержания)
```
fabric · scope · preset
holes: {O1,O2,O3,O4a,O4b,O5(,O0,O6,O7)}  total
by_severity: {blocker,major,minor}
spec_ready / codegen_ready: yes|no
остаток по типу: {вне-scope / противоречие / недоведённый-негатив}
top_holes: [{ось, элемент, что, severity}]
```
Блокеры → человеку на hard-gate. Метрики выносимы без содержания (приёмка, отчёт).
