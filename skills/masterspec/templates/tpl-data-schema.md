---
type: data-schema
slug: data-<domain>
factory: <factory-slug>
status: draft            # draft / actual / deprecated
owner: <team or person>
updated: YYYY-MM-DD
---
# Схема данных: <домен>

<!-- СЛОЙ: спецификации. Логическая схема данных — какие данные система должна
     хранить для выполнения своих функций.
     Может вестись в .md (этот шаблон) или в .yaml/.json (JSON Schema).
     ЗАПРЕЩЕНО:
       - конкретные СУБД, таблицы, колонки, индексы, миграции;
       - СУБД-типы (varchar(255), int4, jsonb);
       - имена ORM-классов.
     Типы атрибутов указывать логически: «строка», «число», «дата-время», «идентификатор», «сумма».
     Эти детали живут в dmap- (слой кода).
     Ссылки — вверх на cdm- и в пределах слоя на cmp-, api-, alg-.
     НЕ ссылаться на dmap-.
     HTML-комментарии удалить при заполнении. -->

## Назначение
<!-- Какие функции опираются на эти данные. -->

## Сущности
<!-- Логические сущности, которые система хранит. Не таблицы. -->
- <сущность> — <зачем нужна>

## Ключевые атрибуты
<!-- По каждой сущности — значимые атрибуты с логическим типом и смыслом. -->

## Связи между сущностями
<!-- Кратность и семантика связи. -->

## Жизненный цикл данных
<!-- Создание → изменение → архивация → удаление. Условия и триггеры переходов. -->

## Ограничения
<!-- Логические ограничения: уникальность, обязательность, диапазоны, TTL по смыслу.
     БЕЗ упоминания индексов/constraint’ов уровня СУБД. -->

## Связи
- Концептуальная модель: -> cdm-...
- Компоненты: -> cmp-...
- Алгоритмы: -> alg-...
