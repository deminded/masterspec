---
type: test-integration
slug: tc-int-<slug>
factory: <factory-slug>
status: draft            # draft / actual / deprecated
owner: <team or person>
updated: YYYY-MM-DD
---
# Интеграционный тест: <название>

<!-- СЛОЙ: спецификации. Интеграционный (сценарный) тест — проверяет, что кооперация
     компонентов, описанная в сценарии, даёт ожидаемый результат.
     Может ссылаться на компоненты и их возможности, но НЕ на код.
     ЗАПРЕЩЕНО:
       - код, имена классов/методов;
       - пути файлов, пакеты;
       - конкретные библиотеки/фреймворки.
     Допустимо: перечислять заглушки/моки ВНЕШНИХ систем в бизнес-терминах.
     Ссылки — вверх на scn-, fn- и в пределах слоя на cmp-, api-, data-.
     НЕ ссылаться на cmap-, trace-, dmap-.
     HTML-комментарии удалить при заполнении. -->

## Проверяет
<!-- Сценарий и (опционально) конкретный путь в нём (happy / ветвление / ошибка). -->
- Сценарий: -> scn-...
- Функция: -> fn-...
- Путь: основной / ветвление «...» / ошибка «...»

## Предусловия
<!-- Состояние компонентов и данных перед стартом.
     Поведение внешних систем (заглушки) — описать логически:
     «внешний комплаенс возвращает OK за < 100 мс». -->

## Входные данные
<!-- Что подаётся на вход в точке триггера сценария. -->

## Шаги
<!-- Пронумерованные шаги, как в сценарии, с уточнениями для теста. -->
1. <что делает тест> — компонент: -> cmp-...
2. ...

## Ожидаемые эффекты
<!-- Наблюдаемые результаты на границах компонентов:
       - какие сообщения/ответы получают потребители;
       - какие события публикуются;
       - какое состояние данных возникает (в терминах логических сущностей).
     Без упоминания таблиц/колонок БД. -->

## Проверяемые условия
<!-- Конкретные утверждения, которые тест проверяет.
     Формулировка: «компонент X возвращает Y», «событие Z опубликовано». -->
- ...

## Связи
- Сценарий: -> scn-...
- Функция: -> fn-...
- Компоненты: -> cmp-...
- API: -> api-...
