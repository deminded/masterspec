<!--
Шаблон change.md для мета-модели masterspec. Все HTML-комментарии — инструкции,
удаляй ПОСЛЕ заполнения. Обязательны: шапка со статусом, §1 Мотивация,
§2 Затронутые артефакты (хотя бы одна непустая таблица), §8 Критерии приёмки.
Остальные секции — если затрагиваются; иначе ставь `Нет изменений.`
-->

# Change: <kebab-name>

> **Статус**: На согласовании
>
> **Дата создания**: YYYY-MM-DD
>
> **Автор**: <ФИО>
>
> **Версия**: 1.0
>
> **Фабрика**: <factory-slug>
>
> **Целевой индекс**: masterspec/00-masterspec-index.md

---

## 1. Мотивация

### 1.1. Цель изменения
<!-- Одна фраза: что меняется, в чём выгода. Без упоминания реализации. -->

### 1.2. Инициатор
<!-- Кто инициировал: бизнес-заказчик, регулятор, тех.долг, инцидент. -->

### 1.3. Приоритет
<!-- критичный | высокий | средний | низкий -->

### 1.4. Слой(и) изменения
<!-- Поставь [x] на затронутых слоях. -->

- [ ] требования (`01-requirements/`)
- [ ] спецификации (`02-specifications/`)
- [ ] кодовая база (`03-codemap/`)
- [ ] решения (`04-decisions/`)

### 1.5. Контекст
<!-- Опционально: ссылки на инцидент / ADR / feature request / bug. -->

---

## 2. Затронутые артефакты

<!--
Три таблицы: MODIFIED (правим существующее), ADDED (новые файлы целиком),
REMOVED (удаляем). Одна строка = один slug. Для ADDED файл должен быть
создан в `new/<slug>.md` рядом с этим change.md. Правила выбора diff vs new/ —
см. ../references/change-format.md.
-->

### 2.1. MODIFIED (diff-правки в §4)

| # | Тип | Slug | Путь | Что меняется (одна строка) |
|---|-----|------|------|-----------------------------|
|   |     |      |      |                             |

### 2.2. ADDED (новые артефакты, файлы в `new/`)

| # | Тип | Slug | Путь (куда будет положен) | Шаблон |
|---|-----|------|---------------------------|--------|
|   |     |      |                           |        |

### 2.3. REMOVED

| # | Тип | Slug | Путь | Причина удаления |
|---|-----|------|------|-------------------|
|   |     |      |      |                   |

---

## 3. Обратная совместимость

<!-- да | нет. Если нет — указать план миграции и breaking-risk. -->

---

## 4. MODIFIED — diff-блоки

<!--
Для каждой строки из §2.1 — один блок ниже.
apply-change парсит блок и применяет правку к целевому файлу:
- modify-bullet: ищем текст из блока ДО в разделе, заменяем на ПОСЛЕ
- replace-section: целиком заменяем содержимое раздела (по заголовку)
- add-subsection: вставляем новую подсекцию в указанный раздел

Раздел адресуем по ЗАГОЛОВКУ (## ... / ### ...), не по номеру.
-->

### 4.1. <slug> — <что меняем>

**Файл**: `01-requirements/02-functions/fn-<slug>.md`
**Раздел**: `## Критерии приёмки`
**Тип правки**: modify-bullet

ДО:
```
- AC-03. <старая формулировка>
```

ПОСЛЕ:
```
- AC-03. <новая формулировка>
```

<!-- Повторяй блоки §4.N для каждой строки MODIFIED. Если MODIFIED нет — `Нет изменений.` -->

---

## 5. ADDED — новые файлы

<!--
Для каждой строки из §2.2 — файл `new/<slug>.md` рядом с этим change.md.
YAML-фронтматтер обязателен. Статус в new/: артефакты слоёв — `draft`;
`adr-` — `proposed`; `dr-` — `accepted`.
При apply-change файл копируется в целевую директорию по `type:` фронтматтера
(таблица — masterspec/references/artifact-routing.md); статус артефактов слоёв
становится `actual`, у `adr-`/`dr-` сохраняется.

Здесь продублируй список для удобства ревью.
-->

- `new/<slug>.md` — <назначение>

<!-- Если ADDED нет — `Нет изменений.` -->

---

## 6. REMOVED — артефакты к удалению

<!-- Повтор из §2.3 с обоснованием удаления каждого slug'а.
Если REMOVED нет — `Нет изменений.` -->

---

## 7. Влияние на направление ссылок

<!--
Если change меняет ссылки между артефактами — перечисли новые и удалённые связи.
Обязательная проверка: НЕ появляются ли обратные ссылки (сверху вниз).

Правило: ссылки идут снизу вверх (код → спеки → требования).
См. masterspec/references/layer-discipline.md § 4.
-->

- Новая ссылка: `<slug-от>` → `<slug-куда>` (<в пределах слоя или снизу вверх>)
- Удалённая ссылка: ...

<!-- Если влияния на ссылки нет — `Нет изменений.` -->

---

## 8. Критерии приёмки изменения

<!-- Чек-лист для закрытия change после apply-change. -->

- [ ] `masterspec/00-masterspec-index.md` перегенерирован полностью (выполняет `apply-change`, не вручную)
- [ ] Нет обратных ссылок (требования → спецификации/код)
- [ ] Все новые slug'и уникальны в пределах фабрики
- [ ] У применённых артефактов слоёв `status: actual`; у `adr-`/`dr-` — свой статус (`proposed`/`accepted`); `updated:` = сегодня
- [ ] У всех MODIFIED-файлов обновлено поле `updated:`
- [ ] Все AC функций, затронутых change, проверены на покрытие `tc-acc-*`
