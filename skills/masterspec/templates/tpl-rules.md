---
type: rules
slug: rules-<domain>
factory: <factory-slug>
status: draft            # draft / actual / deprecated
immutable: false         # true = неизменяемое (регуляторка/контракт/ключевой инвариант); false = пересматриваемое
owner: <team or person>
updated: YYYY-MM-DD
---
# Правила: <домен>

<!-- СЛОЙ: требования. Правила фиксируют, ЧТО должно соблюдаться.
     Если нужно зафиксировать ПОЧЕМУ выбран именно такой подход — это ADR.
     ЗАПРЕЩЕНО: имена компонентов, классов, таблиц, технологий.
     Ссылки — на fn-, as-, cdm-. НЕ ссылаться на cmp-, scn-, alg-, data-.
     HTML-комментарии удалить при заполнении.

     Поле `immutable`:
     - true  — правило продиктовано извне и не пересматривается фабрикой:
               регуляторное требование, контракт с внешней системой, ключевой
               инвариант домена. Противоречащее требование — блок (CONFLICT).
     - false — обычное правило, установленное продуктом/командой; при появлении
               противоречащего требования рассматривается как эволюция фабрики
               (EVOLUTION), а не блок.
     По умолчанию (поле отсутствует) — трактуется как `false` (мягкое). -->

## Бизнес-правила
<!-- Правила, продиктованные бизнесом, регуляторами, договорами.
     Формулируй абстрактно: «Сумма перевода не может превышать дневной лимит клиента». -->

## Инженерные правила
<!-- Внутренние принципы и ограничения уровня требований:
     порядок проверок, инварианты целостности, требования к идемпотентности.
     БЕЗ упоминания технологий и компонентов. -->

## Внешние ограничения (если есть)
<!-- Обязательные внешние требования: регулятор предписывает конкретный алгоритм,
     законодательство требует хранения N лет и т.п. Только действительно внешние. -->

## Принципы принятия решений
<!-- На какие критерии опираться при конфликтах требований. -->

## Связи
- Функции: -> fn-...
- Паспорт: -> as-...
