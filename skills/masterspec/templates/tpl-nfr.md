---
type: nfr
slug: nfr-<scope>
factory: <factory-slug>
status: draft            # draft / actual / deprecated
immutable: false         # true = неизменяемое (SLA по контракту/регуляторный порог); false = пересматриваемое
owner: <team or person>
updated: YYYY-MM-DD
---
# НФТ: <область>

<!-- СЛОЙ: требования. Нефункциональные требования — измеримые характеристики,
     которым система должна соответствовать.
     Каждое требование: ID + формулировка с числом + ссылка на функцию.
     ЗАПРЕЩЕНО:
       - названия технологий (PostgreSQL, Kafka, Redis и т.п.);
       - имена компонентов, классов, таблиц;
       - ограничения на техстек (если они есть — это rules-, а не nfr-).
     Ссылки — на fn-, as-. НЕ ссылаться на cmp-, lp-, api-, data-.
     HTML-комментарии удалить при заполнении.

     Поле `immutable`:
     - true  — порог зафиксирован контрактом с внешней системой, регуляторным
               документом или SLA, по которому фабрика отвечает перед
               потребителем. Пересмотр невозможен без смены внешнего договора.
     - false — внутренний целевой показатель; при появлении требования с
               другим значением — пересматриваем.
     По умолчанию (поле отсутствует) — трактуется как `false` (мягкое). -->

## Производительность
<!-- Каждая метрика числовая и с КВАЛИФИКАТОРАМИ: профиль нагрузки (пиковая/средняя), окно измерения, условие/конфигурация. Без них метрика не доходит до расчёта мощностей.
     Пример: NFR-PERF-01: Проверка допустимости перевода (-> fn-check-transfer) завершается за < 300 мс (p99) при пиковой нагрузке 1000 запросов/с, окно измерения 1 мин. -->
- NFR-PERF-01: ...

## SLA / SLO
<!-- Доступность, время восстановления. -->
- NFR-SLA-01: ...

## Безопасность
<!-- Требования к защите данных и доступу — в терминах классов данных и операций,
     не конкретных технологий. -->
- NFR-SEC-01: ...

## Отказоустойчивость
<!-- Поведение при сбоях внешних зависимостей и внутренних отказах. -->
- NFR-HA-01: ...

## Трассируемость и наблюдаемость
<!-- Требования к логированию, аудиту, корреляции. -->
- NFR-OBS-01: ...

## Ограничения по данным
<!-- Классы данных, правила хранения, маскирования, ротации — логически, без СУБД. -->
- NFR-DATA-01: ...

## Связи
- Паспорт: -> as-...
- Функции: -> fn-...
