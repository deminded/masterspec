---
type: masterspec-index
factory: <factory-slug>
updated: YYYY-MM-DD
---
# Индекс фабрики: <название>

## 1. Паспорт
- Фабрика:
- Владелец:
- Дата создания индекса:
- Статус описания: draft / in-progress / actual

---

## 2. Общие артефакты
 ? `00-glossary.md` # Глоссарий

---

## 3. Слой требований

### 3.1. Паспорт АС/ФП
 ? `01-requirements/01-system/as-<slug>.md` # <название>

### 3.2. Функции АС/ФП

<!-- При большом числе функций группируйте по блокам:
     #### <Название блока>
     Блок = логическая область ответственности (приём, проверки, исполнение...). -->

#### <Функциональный блок>
 ? `01-requirements/02-functions/<block>/fn-<slug>.md` # <название функции>

### 3.3. Нефункциональные требования
 ? `01-requirements/03-nfr/nfr-<scope>.md` # <область>

### 3.4. Бизнес- и внутренние правила
 ? `01-requirements/04-rules/rules-<domain>.md` # <домен>

### 3.5. Диаграмма окружения и функциональная диаграмма
 ? `01-requirements/05-landscape/context-<slug>.md` # Диаграмма окружения
 ? `01-requirements/05-landscape/fd-<slug>.md` # Функциональная диаграмма

### 3.6. Концептуальная модель данных
 ? `01-requirements/06-data-model/cdm-<domain>.md` # <домен>

### 3.7. Справочники и классификаторы
<!-- Только осмысленные для документации: малое число значений, определены
     предметной областью, меняются редко. -->
 ? `01-requirements/07-dictionaries/dict-<slug>.md` # <название>

### 3.8. Приёмочные тесты
<!-- Проверяют критерии приёмки функций АС/ФП. Чёрный ящик. -->
 ? `01-requirements/08-test-cases/tc-acc-<slug>.md` # <название> (-> fn-...)

---

## 4. Слой спецификаций

### 4.1. Компоненты и их возможности

<!-- Возможности (cap-*) описываются ВНУТРИ компонента в разделе «Возможности».
     В индексе перечисляются с отступом под своим компонентом — как справка. -->

 ? `02-specifications/01-components/cmp-<slug>.md` # <название компонента>
   · cap-<name> — <что делает>
   · cap-<name> — <что делает>

### 4.2. Сценарии
 ? `02-specifications/02-scenarios/scn-<slug>.md` # <название сценария>

### 4.3. Алгоритмы
 ? `02-specifications/03-algorithms/alg-<slug>.md` # <название алгоритма>

### 4.4. Внутренние API
 ? `02-specifications/04-apis/internal/api-<slug>.md|yaml|json` # <назначение>

### 4.5. Внешние API
 ? `02-specifications/04-apis/external/api-<slug>.md|yaml|json` # <назначение>

### 4.6. Схемы данных
 ? `02-specifications/05-data/data-<domain>.md|yaml|json` # <домен>

### 4.7. Диаграммы
 ? `02-specifications/06-diagrams/cd-<slug>.md` # Компонентная диаграмма

### 4.8. Профили нагрузки
 ? `02-specifications/07-load-profiles/lp-<scope>.md` # <область>

### 4.9. Интеграционные тесты
<!-- Проверяют работоспособность сценариев. -->
 ? `02-specifications/08-test-cases/tc-int-<slug>.md` # <название> (-> scn-...)

---

## 5. Слой кодовой базы (LLD)

<!-- Генерируется агентом. Не ведётся вручную. -->

### 5.1. Карта репозиториев
 ? `03-codemap/00-repo-map.md` # Обзор репозиториев и стека

### 5.2. Component Maps
 ? `03-codemap/01-component-maps/cmap-<slug>.md` # <компонент> -> код

### 5.3. Scenario Traces
 ? `03-codemap/02-scenario-traces/trace-<slug>.md` # <сценарий> -> цепочка вызовов

### 5.4. Data Maps
 ? `03-codemap/03-data-maps/dmap-<domain>.md` # <домен> -> таблицы/ORM

---

## 6. Решения (ADR)
 ? `04-decisions/adr-NNN-<slug>.md` # <название решения>

---

## 7. Белые пятна и открытые вопросы
- ...

---

### Легенда
 + done — файл создан, содержание актуально
 - draft — файл создан, содержание неполное
 ? not started — файл не создан
 · (точка) — возможность внутри компонента, не отдельный файл
