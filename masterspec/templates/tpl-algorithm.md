---
type: algorithm
slug: alg-<slug>
factory: <factory-slug>
status: draft            # draft / actual / deprecated
owner: <team or person>
updated: YYYY-MM-DD
form: procedural         # procedural (дефолт) / decision-table (таблица решений, DMN) — см. § «Правила»
# sidecar_format:        # только для decision-table с машинным DMN-движком: dmn-1.x
# sidecar:               # <slug>.dmn — patterns/sidecar-formats.md
---
# Алгоритм: <название>

<!-- СЛОЙ: спецификации. Пошаговая логика обработки, ветвления, порядок проверок, инварианты.
     ЗАПРЕЩЕНО:
       - код, псевдокод на языке программирования;
       - имена классов/методов/файлов;
       - конкретные технологии;
       - маппинги полей контракта (поле ← источник) — они часть КОНТРАКТА (api), не алгоритма;
         алгоритм описывает логику обработки, а не наполнение контракта (single-source O0).
     Шаги описываются словами, как бизнес-логика.
     Ссылки — вверх на rules-, fn- и в пределах слоя на scn-, cmp-.
     НЕ ссылаться на trace-.
     ФОРМА раздела «Правила» зависит от form (фронтматтер):
       - procedural (дефолт) — пронумерованный перечень правил словами;
       - decision-table — таблица «условия → результат» + Hit policy (DMN-семантика).
     HTML-комментарии удалить при заполнении. -->

## Контекст применения
<!-- Где и когда этот алгоритм используется: в каком компоненте, в каком сценарии. -->

## Вход
<!-- Что принимается на вход (логически). -->

## Выход
<!-- Что получается на выходе (логически). -->

## Правила
<!-- ФОРМА по атрибуту form (фронтматтер).

     form: procedural — пронумерованный перечень правил/шагов словами.
     ИНВАРИАНТ ПОЛНОТЫ ВЕТВЛЕНИЙ: каждое условное правило («если X») имеет явную ветку
     «иначе» (или пометку «иначе — невозможно, потому что…»). Условие с одним исходом = дыра. -->
1. ...
2. ...
3. ...

<!-- form: decision-table — таблица решений (DMN-семантика). Колонки-условия (входы) + последняя
     колонка-результат; каждая строка = правило. Форма — таблица, не проза; это МОДЕЛЬ решений,
     не псевдокод — слой спецификаций остаётся code-free.

     **Hit policy** ОБЯЗАТЕЛЬНА (какое правило выигрывает при пересечении условий):
       unique — строки взаимоисключающи и полны; first — приоритет по порядку строк;
       priority — явный приоритет.
     ИНВАРИАНТ ПОЛНОТЫ: все комбинации входов покрыты строкой или есть явная default-строка
       (непокрытая комбинация = дыра, аналог пустой ячейки матрицы состояний).

     Пример:
       | Условие A | Условие B | Результат |
       |---|---|---|
       | да        | —         | X         |
       | нет       | да        | Y         |
       | нет       | нет       | Z         |
       **Hit policy:** unique
-->


## Порядок проверок
<!-- Если порядок важен — зафиксируй его и обоснуй (или сошлись на ADR). -->

## Причины выбора порядка
<!-- Если выбор неочевиден — ссылка на ADR: -> adr-NNN-... -->

## Инварианты
<!-- Условия, которые должны оставаться истинными до, во время и после. -->

## Связи
- Правила: -> rules-...
- Сценарии: -> scn-...
- Компоненты: -> cmp-...
- ADR: -> adr-...
