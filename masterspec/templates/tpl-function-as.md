---
type: function
slug: fn-<slug>
factory: <factory-slug>
status: draft            # draft / actual / deprecated
criticality: <high | medium | low> # вес покрытия: high=5, medium=3, low=1
owner: <team or person>
updated: YYYY-MM-DD
---
# Функция: <название>

<!-- СЛОЙ: требования. Одна функция — одна полезность АС/ФП, описанная как use-case
     в терминах «актор — система — результат».
     ЗАПРЕЩЕНО:
       - имена компонентов, сервисов, адаптеров;
       - порядок вызова внутренних компонентов (это — в scn-, не здесь);
       - имена классов, методов, таблиц, полей, API-путей;
       - технологии, протоколы, СУБД, брокеры.
     Ссылки — только на артефакты своего слоя (fn, rules, nfr, cdm, as).
     НЕ ссылаться на cmp-, cfn-, scn-, alg-, api-, data-, lp-.
     HTML-комментарии удалить при заполнении. -->

## Цель
<!-- Зачем эта функция нужна бизнесу / потребителю. Одним абзацем. -->

## Инициатор
<!-- Кто запускает функцию: роль пользователя, внешняя система, расписание, событие. -->
- **Внешний инициатор/канал:** <реальный актор/внешняя система/канал | N/A — internal-only>
<!-- Укажи внешний канал и при внутреннем триггере (например, расписание читает внешний источник).
     Это поле детерминированно включает OE-гейт. -->

## Триггер
<!-- Что именно приводит к запуску: поступление запроса, наступление времени, событие. -->

## Предусловия
<!-- Что должно быть истинно ДО запуска функции (состояние системы/данных). -->

## Основной поток
<!-- Use-case happy path. Формат: «1. Актор делает X. 2. Система проверяет Y. 3. Система возвращает Z».
     Пиши в терминах взаимодействия актор ↔ система, БЕЗ упоминания внутренних компонентов. -->
1. ...
2. ...
3. ...

## Альтернативные потоки
<!-- Условие → поведение системы. Без внутренней реализации. -->

## Исключительные потоки (ошибки, отказы)
<!-- Возможные отказы и реакция системы, наблюдаемая потребителем. -->

## Результат
<!-- Что функция возвращает / производит при успешном выполнении. -->

## Постусловия
<!-- Что становится истинным в системе/данных после успешного выполнения. -->

## Контракт живой эксплуатации (OE)
<!-- Для функции БЕЗ внешнего I/O удали весь скелет ниже и оставь ровно:
     OE: N/A — internal-only
     Для функции с внешним актором/каналом все восемь подразделов присутствуют ровно один раз.
     Полный контракт и вопросы: ../references/operational-envelope.md.
     Для каждого заполни:
       Статус: APPLICABLE | N/A — <предметная причина> | OPEN — <кому и какой вопрос>
       Контракт: конкретные границы/варианты либо ссылки на rules-/nfr-/context- своего слоя
       Основание: измерение/интервью/обезличенный реальный образец/vendor-документация
       Проверка: AC-... для APPLICABLE; N/A для N/A
     Пусто и «не применимо» без причины = blocker. OPEN = blocker spec_ready, а не разрешение
     домыслить. Стабильная ссылка из нижних слоёв: -> fn-<slug>/OE-<ID>. -->

### OE-LOAD — объём и ритм
- **Статус:** <APPLICABLE | N/A — причина | OPEN — вопрос>
- **Контракт:** <!-- всего объектов / активно или изменяется за интервал / частота запуска /
  типичный, пиковый и предельный объём / backlog после простоя / ожидаемый рост. Не описывать алгоритм. -->
- **Основание:**
- **Проверка:** <AC-... | N/A>

### OE-INPUT — реальные формы входа
- **Статус:** <APPLICABLE | N/A — причина | OPEN — вопрос>
- **Контракт:** <!-- Классы реальных производителей + формы после преобразований клиента/канала. -->
- **Основание:** <!-- Образец/документация; synthetic допустим с моделью соответствия и residual risk. -->
- **Проверка:** <AC-... | N/A>

### OE-EVIDENCE — наблюдаемый исход и след
- **Статус:** <APPLICABLE | N/A — причина | OPEN — вопрос>
- **Контракт:** <!-- Успех/отказ/ожидание + долговечный пользовательский след без внутренних логов. -->
- **Основание:**
- **Проверка:** <AC-... | N/A>

### OE-SOURCES — классы источников и политика
- **Статус:** <APPLICABLE | N/A — причина | OPEN — вопрос>
- **Контракт:** <!-- Все классы, включая автоматизированных акторов; all/none/selected,
  default и поведение неизвестного класса. Общую формулу не дублировать — сослаться на rules-. -->
- **Основание:**
- **Проверка:** <AC-... | N/A>

### OE-SECURITY — конфиденциальность и целостность
- **Статус:** <APPLICABLE | N/A — причина | OPEN — вопрос>
- **Контракт:** <!-- Аутентификация/авторизация, валидация входа, шифрование и подпись на границах
  функции. Общую политику не копировать: сослаться на rules-/nfr-. -->
- **Основание:**
- **Проверка:** <AC-... | N/A>

### OE-RESILIENCE — повтор, отказ и восстановление
- **Статус:** <APPLICABLE | N/A — причина | OPEN — вопрос>
- **Контракт:** <!-- Идемпотентность/дедуп, retry/backoff, частичный успех, рестарт, резервирование.
  Заполняется и вне сценариев доставки. -->
- **Основание:**
- **Проверка:** <AC-... | N/A>

### OE-DELIVERY — сквозной путь результата
- **Статус:** <APPLICABLE | N/A — причина | OPEN — вопрос>
- **Контракт:** <!-- Где заканчивается «доставлено» для потребителя; внешний путь и предпосылки,
  точки подтверждения, silent drop, retry/escalation/reconciliation. Топологию вынести в context-. -->
- **Основание:**
- **Проверка:** <AC-... | N/A>

### OE-CONTROL — живой управляющий ввод
- **Статус:** <APPLICABLE | N/A — причина | OPEN — вопрос>
- **Контракт:** <!-- Грамматика, нормализация, контексты ввода, неоднозначность и явный отказ. -->
- **Основание:**
- **Проверка:** <AC-... | N/A>

## Критерии приёмки
<!-- Каждый критерий — проверяемый ОРАКУЛ в форме given-when-then (дано → когда → тогда), с числом/фактом в «тогда». Не «обрабатывает корректно», а конкретный проверяемый результат. Покрой и альтернативные/исключительные потоки.
     Каждый APPLICABLE OE должен быть привязан минимум к одному AC; один AC может покрывать
     несколько граней, если оракул явно проверяет каждую.
     Формат: AC-<ID>: дано <предусловие>, когда <действие>, тогда <проверяемый результат>.
     Пример: AC-01: дано валидный запрос, когда отправлен, тогда результат возвращается за < 500 мс (p99). -->
- AC-01: дано ..., когда ..., тогда ...

## Связи
- Правила: -> rules-...
- НФТ: -> nfr-...
- Окружение: -> context-...
- Концептуальная модель: -> cdm-...
