---
type: scenario
slug: scn-<slug>
factory: <factory-slug>
status: draft            # draft / actual / deprecated
owner: <team or person>
updated: YYYY-MM-DD
---
# Сценарий: <название>

<!-- СЛОЙ: спецификации. ЕДИНСТВЕННОЕ место, где фиксируется ПОРЯДОК ВЫЗОВА
     компонентов для реализации одной функции АС/ФП — от триггера до результата.
     Каждый шаг привязывается к компоненту и к конкретной его возможности
     в формате: -> cmp-<slug>/cap-<name>
     ЗАПРЕЩЕНО:
       - код, имена классов/методов, пути файлов;
       - конкретные технологии;
       - имена полей/таблиц.
     Ссылки — вверх на fn- и в пределах слоя на cmp-, alg-, api-.
     НЕ ссылаться на cmap-, trace-, dmap-.
     HTML-комментарии удалить при заполнении. -->

## Цель сценария
<!-- Какую функцию АС/ФП реализует, коротко. -->

## Триггер
<!-- Что запускает: запрос от актора, событие, расписание. -->

## Предусловия
<!-- Что должно быть истинно до старта. -->

## Последовательность шагов
<!-- Happy path. Формат каждой строки:
       N. <что происходит>. -> cmp-<slug>/cap-<name>
     Описывай действие, а не вызов метода.
     Пример: «3. Выбрать канал доставки по типу получателя. -> cmp-notification-router/cap-route-by-channel».
     ОДНА ИНТЕГРАЦИЯ — ОДИН ШАГ: каждый отдельный внешний вызов/интеграция = свой шаг,
     не схлопывай несколько внешних взаимодействий в один логический «вызов» (иначе теряется
     участник и его контракт). Каждый шаг с внешним вызовом ссылается на свой -> api-<slug>. -->
1. ...
2. ...
3. ...

## Ветвления
<!-- Альтернативные пути: условие → что происходит, какая возможность какого компонента
     задействована. Формат ссылок тот же: -> cmp-<slug>/cap-<name>.
     ИНВАРИАНТ ПОЛНОТЫ: каждое условие/решение из happy-path имеет здесь ≥2 явных исхода
     (нет молчаливо опущенной else-ветки). Развилка с одним описанным исходом = дыра,
     как пустая ячейка матрицы состояний. -->

## Ошибки и таймауты
<!-- Поведение при сбоях: компенсация, retry, fallback (логически, без технологий). -->

## Постусловия
<!-- Что становится истинным после успешного завершения. -->

## Реализация контракта живой эксплуатации
<!-- Перечислить ВСЕ APPLICABLE OE-грани реализуемой функции. Union таких таблиц по сценариям
     функции должен полностью покрыть её OE. Владелец воплощения — этот сценарий или конкретный
     alg-/api-/data-/lp-/ui-view-. «Будет учтено в коде» без владельца = blocker. -->
| Грань требования | Шаг / ветка / ошибка | Владелец механизма | Проверка |
|---|---|---|---|
| -> fn-.../OE-LOAD | | -> lp-... | -> tc-int-... |
| -> fn-.../OE-... | | -> alg-... / -> api-... / -> data-... / -> ui-view-... / этот сценарий | -> tc-int-... |

## Связи
- Функция АС/ФП: -> fn-...
- Участвующие компоненты: -> cmp-...
- Алгоритмы: -> alg-...
- API: -> api-...
- Профили нагрузки: -> lp-...
- Интеграционные тесты: -> tc-int-...
