---
type: data-schema
slug: data-<domain>
factory: <factory-slug>
status: draft            # draft / actual / deprecated
owner: <team or person>
updated: YYYY-MM-DD
sidecar_format: json-schema-2020-12   # ОБЯЗАТЕЛЬНО: формат машинной схемы
sidecar: data-<domain>.schema.json    # ОБЯЗАТЕЛЬНО: файл со СТРУКТУРОЙ (сущности, атрибуты, типы)
contract_origin: authored             # authored | imported (втянута извне)
---
# Схема данных: <домен>

<!-- СЛОЙ: спецификации. Логическая схема данных — какие данные система должна
     хранить для выполнения своих функций.
     Может вестись целиком в .md (этот шаблон) ИЛИ как машинная схема .yaml/.json (JSON Schema).
     Машинный режим (компаньон, см. change-format.md §3.1): схема — отдельный файл <slug>.yaml,
     а ЭТОТ .md — обязательный компаньон рядом (фронтматтер + назначение + ограничения +
     строка «Машинная спека: <slug>.yaml»). Индексация/verify/мерж идут по этому .md.
     ЗАПРЕЩЕНО:
       - конкретные СУБД, таблицы, колонки, индексы, миграции;
       - СУБД-типы (varchar(255), int4, jsonb);
       - имена ORM-классов.
     Типы атрибутов указывать логически: «строка», «число», «дата-время», «идентификатор», «сумма».
     Эти детали живут в dmap- (слой кода).
     Ссылки — вверх на cdm- и в пределах слоя на cmp-, api-, alg-.
     НЕ ссылаться на dmap-.
     HTML-комментарии удалить при заполнении. -->

## Назначение
<!-- Какие функции опираются на эти данные. -->

## Сущности — назначение
<!-- НЕ повтор схемы. Структура (сущности, атрибуты, типы, обязательность, связи) живёт ТОЛЬКО в
     сайдкаре (JSON Schema), объявленном полем sidecar:. Повтор здесь = вторая нормативная редакция,
     нарушение O0. Здесь — смысл: что за сущность в терминах домена и зачем она системе. -->
| Сущность (из сайдкара) | Назначение в домене |
|---|---|
| <имя> | <≤20 слов> |

## Состояния и переходы
<!-- Матрица «состояние × событие → результат», без пустых ячеек: каждая пара (состояние, входящее событие) даёт определённый ответ ИЛИ помечена «невозможна». Для каждого перехода — триггер и момент (при чтении / по расписанию / при записи). Укажи явно, хранимое состояние или вычисляемое; для вычисляемого — единый источник входов (например, единый источник текущего времени). -->

| Состояние | Событие | → Результат (или ЗАПРЕЩЕНО) | Момент |
|---|---|---|---|
| <состояние> | <событие> | <новое состояние / ЗАПРЕЩЕНО> | при чтении / по расписанию / при записи |

## Ограничения
<!-- Логические ограничения: уникальность, обязательность, диапазоны, TTL по смыслу.
     БЕЗ упоминания индексов/constraint’ов уровня СУБД. -->

## Связи
- Концептуальная модель: -> cdm-...
- Компоненты: -> cmp-...
- Алгоритмы: -> alg-...
