---
type: api
slug: api-<slug>
scope: <internal | external>   # ОБЯЗАТЕЛЬНО: определяет 04-apis/internal или /external (routing + индекс §4.4/§4.5)
boundary: intra-factory        # уровень границы/security: intra-factory | inter-factory | perimeter — references/boundary-registry.md (ортогонален scope)
factory: <factory-slug>
status: draft            # draft / actual / deprecated
owner: <team or person>
updated: YYYY-MM-DD
sidecar_format: <openapi-3.1 | asyncapi-2.x | …>   # ОБЯЗАТЕЛЬНО: формат машинного контракта
sidecar: api-<slug>.<ext>                          # ОБЯЗАТЕЛЬНО: файл со СТРУКТУРОЙ контракта
contract_origin: authored                          # authored (ведём мы) | imported (владелец снаружи)
# contract_source: {uri: <откуда>, revision: <ревизия>, sha256: <дайджест>}   # ТОЛЬКО при imported
---
# API: <название>

<!-- СЛОЙ: спецификации. Артефакт api — ПАРА: машинный сайдкар (структура) + этот компаньон (семантика).
     schema-first: СТРУКТУРА контракта (операции, пути/каналы, схемы вход/выход, типы, транспортные
     статусы) живёт ТОЛЬКО в сайдкаре, объявленном полем sidecar:. Здесь её ПОВТОРЯТЬ ЗАПРЕЩЕНО —
     повтор = вторая нормативная редакция = нарушение O0 (patterns/sidecar-formats.md).
     Компаньон несёт то, чего в OpenAPI/AsyncAPI нет: назначение, бизнес-отказы, маппинги полей,
     ограничения живой эксплуатации, связи вверх.
     Машинный режим (компаньон, см. change-format.md §3.1): машинная спека — отдельный файл
     <slug>.yaml (его потребляют внешние процессы), а ЭТОТ .md — обязательный компаньон рядом:
     несёт фронтматтер (type/slug/scope/status), назначение, ограничения, МАППИНГИ ПОЛЕЙ
     (правила наполнения — логические, в OpenAPI/AsyncAPI не выражаются, потому живут здесь,
     не в .yaml — single-source O0) и строку «Машинная спека: <slug>.yaml».
     Индексация/verify/мерж идут по этому .md.
     ЗАПРЕЩЕНО в .md:
       - конкретные реализации обработчиков (классы, методы, файлы);
       - схемы БД, имена таблиц.
     Конкретные пути/топики указываются как часть КОНТРАКТА (они — часть спецификации, не кода),
     но привязка к файлам/классам — нет.
     Ссылки — вверх на fn- и в пределах слоя на cmp-, scn-, data-.
     НЕ ссылаться на cmap-, trace-.
     HTML-комментарии удалить при заполнении. -->

## Тип API
<!-- Выбери один: internal-sync / internal-async / external-sync / external-async. -->

## Назначение
<!-- Зачем существует этот API, какие потребители. -->

## Операции / события — назначение
<!-- НЕ повтор контракта. Здесь только СМЫСЛ каждой операции: зачем она есть и для кого.
     Идентификаторы берутся из сайдкара (operationId / channel), тело — там же.
     Вход/выход/статусы здесь НЕ описываются: это структура, она в сайдкаре. -->
| operationId (из сайдкара) | Направление (async: produce/consume) | Назначение — зачем и кому |
|---|---|---|
| <id> | <produce \| consume \| —> | <≤20 слов> |

## Business-reject codes
<!-- Машиночитаемый источник для tc-flt/O_T1. Перечисли только реально заданные бизнес-отказы,
     технические 5xx/timeout сюда не входят. Если бизнес-отказов нет, оставь предметное N/A. -->
- **Business-reject codes:** <`CODE_1`, `CODE_2` | N/A — контракт не содержит бизнес-отказов>

## Маппинги полей
<!-- Правила НАПОЛНЕНИЯ полей контракта: откуда берётся значение каждого поля,
     которое не вводится клиентом напрямую. Это часть КОНТРАКТА (декларативное
     соответствие источнику), а НЕ алгоритм обработки — поэтому здесь, в api, а не в alg.
     Для каждого такого поля: <поле> ← <источник> (+ условие/трансформация словами).
     Источник указывается ЛОГИЧЕСКИ (без кода, имён классов/таблиц):
       -> api-<…> (поле другого контракта) | -> data-<…> (поле модели данных) |
       контекст приложения (сессия, конфиг, текущее время) | константа.
     Покрывать обе стороны, где применимо: наполнение полей запроса и формирование полей ответа.
     Поле, требующее наполнения, но без указанного источника, — дыра контракта (ось O6). -->
- <поле запроса/ответа> ← <источник> — <условие/трансформация>

## Ограничения
<!-- Нефункциональные обязательства контракта. -->
- идемпотентность:
- SLA (время ответа, доступность):
- ретраи и их безопасность:
- ограничения по нагрузке:
- версиониование:
- безопасность границы (по `boundary`):
  <!-- Глубина требований растёт по атрибуту boundary (references/boundary-registry.md):
       intra-factory (relaxed) → inter-factory (service-to-service auth) → perimeter (строгий
       внешний профиль). Конкретные протоколы и пороги здесь не перечисляются — они
       ИЛЛЮСТРАТИВНЫМ примером даны в boundary-registry.md и как норма приходят
       security-guardrail-пакетом (masterspec-pack), не из kernel; здесь фиксируется применимый
       профиль и его точки на контракте. -->

## Сквозная доставка и подтверждения
<!-- Это нативная семантика API, не второй OE-маппинг: владельцем реализации остаётся scn-.
     Для external API, участвующего в связанном -> fn-.../OE-DELIVERY, различить «принято внешней
     стороной» и конечный результат. Для неприменимого контракта написать одну предметную N/A-строку. -->
- **Предпосылки внешней стороны и владелец:**
- **Точки подтверждения и значение каждой:**
- **Поздний/молчаливый отказ:**
- **Retry / reconciliation / пользовательское свидетельство:**

## Связи
- Функции: -> fn-...
- Компоненты: -> cmp-...
- Сценарии: -> scn-...
- Схема данных: -> data-...
- Основания OE (только ссылки): -> fn-.../OE-INPUT, -> fn-.../OE-DELIVERY
