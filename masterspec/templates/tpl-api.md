---
type: api
slug: api-<slug>
scope: <internal | external>   # ОБЯЗАТЕЛЬНО: определяет 04-apis/internal или /external (routing + индекс §4.4/§4.5)
factory: <factory-slug>
status: draft            # draft / actual / deprecated
owner: <team or person>
updated: YYYY-MM-DD
---
# API: <название>

<!-- СЛОЙ: спецификации. Контракт взаимодействия — логически.
     Может вестись целиком в .md (этот шаблон) ИЛИ как машинная спека .yaml/.json (OpenAPI/AsyncAPI).
     Машинный режим (компаньон, см. change-format.md §3.1): машинная спека — отдельный файл
     <slug>.yaml (его потребляют внешние процессы), а ЭТОТ .md — обязательный компаньон рядом:
     несёт фронтматтер (type/slug/scope/status), назначение, ограничения, МАППИНГИ ПОЛЕЙ
     (правила наполнения — логические, в OpenAPI/AsyncAPI не выражаются, потому живут здесь,
     не в .yaml — single-source O0) и строку «Машинная спека: <slug>.yaml».
     Индексация/verify/мерж идут по этому .md.
     ЗАПРЕЩЕНО в .md:
       - конкретные реализации обработчиков (классы, методы, файлы);
       - схемы БД, имена таблиц.
     Конкретные пути/топики указываются как часть КОНТРАКТА (они — часть спецификации, не кода),
     но привязка к файлам/классам — нет.
     Ссылки — вверх на fn- и в пределах слоя на cmp-, scn-, data-.
     НЕ ссылаться на cmap-, trace-.
     HTML-комментарии удалить при заполнении. -->

## Тип API
<!-- Выбери один: internal-sync / internal-async / external-sync / external-async. -->

## Назначение
<!-- Зачем существует этот API, какие потребители. -->

## Операции / события
<!-- Перечень логических операций с описанием назначения каждой.
     Для АСИНХРОННЫХ (события/топики) направление — ПЕРВИЧНЫЙ атрибут: produce (публикует)
     или consume (потребляет). Без направления async-контракт нечитаем — потребитель не
     поймёт, слушать ему топик или писать в него. -->
- <операция/событие> [produce|consume для async] — <назначение>

## Контракт
<!-- Для каждой операции / события — ОБЕ СТОРОНЫ, чтобы клиента можно было написать ТОЛЬКО по контракту:
       - идентификатор (endpoint / topic / method name на логическом уровне);
       - ВХОД: формат запроса (логические поля, без типов БД), обязательность полей, значимые заголовки;
       - ОТВЕТ-УСПЕХ: статус успеха + форма ответа (логические поля);
       - ОТВЕТ-ОТКАЗ: классы ошибок со своими статусами, в том числе порождённые ограничениями (например, отсутствует обязательный заголовок → отказ). Различимые по смыслу исходы дают различимые ответы (например, «не найдено» ≠ «истекло»). -->
- <операция>: вход → ответ-успех → ответ-отказ

## Маппинги полей
<!-- Правила НАПОЛНЕНИЯ полей контракта: откуда берётся значение каждого поля,
     которое не вводится клиентом напрямую. Это часть КОНТРАКТА (декларативное
     соответствие источнику), а НЕ алгоритм обработки — поэтому здесь, в api, а не в alg.
     Для каждого такого поля: <поле> ← <источник> (+ условие/трансформация словами).
     Источник указывается ЛОГИЧЕСКИ (без кода, имён классов/таблиц):
       -> api-<…> (поле другого контракта) | -> data-<…> (поле модели данных) |
       контекст приложения (сессия, конфиг, текущее время) | константа.
     Покрывать обе стороны, где применимо: наполнение полей запроса и формирование полей ответа.
     Поле, требующее наполнения, но без указанного источника, — дыра контракта (ось O6). -->
- <поле запроса/ответа> ← <источник> — <условие/трансформация>

## Ограничения
<!-- Нефункциональные обязательства контракта. -->
- идемпотентность:
- SLA (время ответа, доступность):
- ретраи и их безопасность:
- ограничения по нагрузке:
- версиониование:

## Связи
- Функции: -> fn-...
- Компоненты: -> cmp-...
- Сценарии: -> scn-...
- Схема данных: -> data-...
