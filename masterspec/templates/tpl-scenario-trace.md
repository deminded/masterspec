---
type: scenario-trace
slug: trace-<scenario-slug>
factory: <factory-slug>
status: draft            # draft / actual / deprecated
updated: YYYY-MM-DD
generated: true          # этот файл генерируется агентом
---
# Code Trace: <название сценария>

<!-- Цепочка вызовов в коде, реализующая сценарий из спецификации.
     Генерируется агентом на основе анализа кода и спецификации сценария.
     Каждый шаг — конкретный вызов с указанием файла и строки.
     Связи указывайте в формате -> slug. -->

## Сценарий
- **Спецификация:** -> scn-...
- **Триггер в коде:** `path/file:line` — <что инициирует>

## Цепочка вызовов

<!-- Пронумерованные шаги = реальный путь выполнения в коде.
     Каждый шаг привязан к шагу из спецификации сценария.
     kind: call, async-send, async-receive, db-query, cache-read, external-call -->

| # | Шаг сценария | Компонент | Code Unit | Kind | Файл | Описание |
|---|---|---|---|---|---|---|
| 1 | | -> cmp-... | `Class.method()` | call | `path/file:line` | |
| 2 | | -> cmp-... | `Class.method()` | call | `path/file:line` | |
| 3 | | -> cmp-... | `Class.method()` | async-send | `path/file:line` | |

## Ветвления в коде

<!-- Где в коде реализованы ветвления из сценария. -->

| Условие (из сценария) | Файл | Строка | Как реализовано |
|---|---|---|---|
| | `path/file` | line | if / when / pattern match / ... |

## Обработка ошибок

<!-- Где в коде обрабатываются ошибки и таймауты из сценария. -->

| Ошибка (из сценария) | Файл | Строка | Механизм |
|---|---|---|---|
| | `path/file` | line | try-catch / fallback / circuit-breaker / ... |

## Связи
- Сценарий: -> scn-...
- Алгоритмы: -> alg-...
- Компоненты: -> cmp-...
- Component maps: -> cmap-...
