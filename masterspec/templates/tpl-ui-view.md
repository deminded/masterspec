<!--
Шаблон UI-view (артефакт слоя спецификаций). Описывает экран/карточку/форму
интерфейса: логический состав элементов, состояния, пользовательские сценарии.

ЗАПРЕЩЕНО в этом артефакте:
- CSS-код, пиксельные размеры, конкретные шрифты/иконки — это слой решений (ADR);
- имена классов/файлов/компонентов фронта (React/Vue/Angular и т.д.) — это codemap;
- конкретные URL-эндпоинты и query-параметры — это слой api (ссылка на api- slug);
- обратные переходы «сюда приходят из ui-view-X» — это фиксируется в `nav-<factory>.md`,
  а не в самом view. Триггер открытия описывается абстрактно (действие пользователя),
  без имени view-источника.

РАЗРЕШЕНО:
- логическое описание элементов (таблица, поле ввода, кнопка, модалка);
- ссылки вверх на fn-* (какие функции реализует view);
- ссылки в пределах слоя спецификаций: на api-* (контракт с backend) и ИСХОДЯЩИЕ
  переходы в другие `ui-view-*` в разделе «Навигация»;
- состояния view (empty / loading / error / success);
- пользовательские шаги взаимодействия.

Удаляй HTML-комментарии перед сохранением.
-->
---
type: ui-view
slug: ui-view-<kebab-case>
factory: <factory-slug>
status: draft
updated: YYYY-MM-DD
---

# UI View: <человекочитаемое имя>

## Назначение

<Одна-две фразы: что это за экран/карточка/форма, в каком контексте открывается.>

## Реализует функции

<!-- Ссылки вверх на fn-*, которые этот view делает доступными пользователю.
     Если view агрегирует несколько функций — перечисли все. -->

- -> fn-<slug>
- -> fn-<slug>

## Триггер открытия

<!-- Абстрактное описание действия пользователя, инициирующего показ.
     НЕ указывай имя view-источника (это backward ref). Карта переходов
     откуда-куда живёт в `nav-<factory>.md`. -->

<Вход на главную / клик по кнопке «<название>» / deeplink / и т.п.>

## Элементы

<!-- Логический состав view. Без CSS, без имён компонентов фронта. -->

### Основной блок

- <элемент 1: что это, роль>
- <элемент 2>

### Панель управления

- Кнопка <название> — <действие, куда ведёт>
- Кнопка <название>

### Дополнительные элементы (опционально)

- <элемент>

## Состояния

<!-- Через какие состояния view проходит и что в каждом показывается. -->

| Состояние | Условие | Что показывается |
|-----------|---------|-------------------|
| empty | Нет данных к показу | <...> |
| loading | Идёт загрузка | <...> |
| error | Ошибка API / валидации | <...> |
| success / default | Данные загружены | <...> |

## Взаимодействия пользователя

<!-- Пошаговые сценарии: что пользователь делает и что происходит.
     Для перехода в другой view используй ссылку `-> ui-view-<slug>`.
     Для вызова backend-функции — ссылка `-> fn-<slug>` с пометкой «действие». -->

### Сценарий: <название>

1. Пользователь <действие> (например: «нажимает кнопку Сохранить»)
2. Система валидирует <...>
3. <Если ОК>: выполняется <действие> (-> fn-<slug>), view переходит в состояние <...>
4. <Если ошибка>: показывается сообщение <...>, view остаётся в <...>

### Сценарий: <название>

<Пошаговое описание>

## Навигация

<!-- Куда может уйти пользователь из этого view. -->

- -> ui-view-<другой view> (по действию <какому>)
- -> ui-view-<другой view> (по ссылке <какой>)

## Ограничения и правила

<!-- Правила валидации полей, ограничения по ролям, правила отображения.
     Можно ссылаться на rules-* и nfr-*. -->

- <правило / ссылка -> rules-<slug>>
- <правило / ссылка -> nfr-<slug>>

## Контракт с backend

<!-- Какие api-* вызываются из view. Не перечисляй конкретные эндпоинты —
     только ссылки на артефакты api-*. -->

- -> api-<slug> (операции <...>)

## Открытые вопросы

<!-- Если что-то не уточнено — фиксируй здесь. -->

- <...>

## Связи

- Паспорт: -> as-<factory-slug>
- Реализует функции: (см. выше)
- Backend-контракт: -> api-<slug>
- Карта переходов: -> nav-<factory-slug>

<!-- Запрещено «Navigation входы: <- ui-view-X» — это обратная ссылка,
     её место в nav-<factory-slug>.md. -->

