---
type: component
slug: cmp-<slug>
factory: <factory-slug>
status: draft            # draft / actual / deprecated
owner: <team or person>
updated: YYYY-MM-DD
---
# Компонент: <название>

<!-- СЛОЙ: спецификации. Компонент — логическая единица фабрики с чёткой границей
     ответственности.
     ВАЖНО: функциональность компонента описывается ЗДЕСЬ в разделе «Возможности»,
     а не в отдельных артефактах. Одна возможность (capability) — это одна
     самостоятельная способность компонента, которая может использоваться
     в нескольких функциях АС/ФП.
     ЗАПРЕЩЕНО:
       - код, классы, методы, файлы, пути;
       - имена таблиц, ORM-сущностей, миграций;
       - конкретные технологии (PostgreSQL, Redis, Kafka, Spring и т.п.).
     Ссылки — вверх на fn-, as- и в пределах слоя на scn-, alg-, api-, data-.
     НЕ ссылаться на cmap-, trace-, dmap-.
     HTML-комментарии удалить при заполнении. -->

## Назначение
<!-- За что отвечает компонент в системе, одним абзацем. -->

## Ответственность
<!-- Исчерпывающий список областей, за которые отвечает компонент. -->

## Не делает
<!-- Явные исключения — что НЕ входит в ответственность. -->

## Возможности (capabilities)

<!-- Стабильный набор способностей компонента (обычно 3–7 штук).
     Каждая возможность:
       - имеет короткое имя (cap-<kebab-case>);
       - делает ровно одну осмысленную вещь;
       - описана в терминах входа/выхода, БЕЗ кода и классов;
       - может использоваться в нескольких функциях АС/ФП.
     Именно возможности ссылаются из сценариев (scn-) и маппятся на код (cmap-).

     Пример для cmp-notification-router:
       - cap-route-by-channel — выбрать канал для уведомления по каналу и типу получателя
       - cap-apply-retry-policy — применить политику повторной отправки при отказе канала
       - cap-track-delivery-status — зафиксировать статус доставки от канального адаптера
 -->

### cap-<name>
- **Что делает:** <одной фразой>
- **Вход (логически):** ...
- **Выход (логически):** ...
- **Используется функциями АС/ФП:** -> fn-..., -> fn-...

### cap-<name>
- **Что делает:**
- **Вход (логически):**
- **Выход (логически):**
- **Используется функциями АС/ФП:** -> fn-...

## Зависимости
<!-- От каких других компонентов и внешних систем компонент зависит
     и зачем. Без имён библиотек и классов. -->
- -> cmp-... — <зачем>
- <внешняя система> — <зачем>

## Связи
- Реализует функции АС/ФП: -> fn-...
- Сценарии: -> scn-...
- Алгоритмы: -> alg-...
- API: -> api-...
- Данные: -> data-...
